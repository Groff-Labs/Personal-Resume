# macOS
.DS_Store

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / local
.vscode/
.idea/
*.swp

# Claude Code local state
.claude/
.claude*
.playwright-mcp/

# Playwright ad-hoc screenshots at repo root (session work product, not code)
/*.png

# Next.js (subdir-specific; frontend also has its own .gitignore)
.next/
out/

# Generated artifacts (rebuilt on every `npm run build` from resume.md)
frontend/public/resume.pdf

# CDK
cdk.out/
.cdk.staging/

# Env / secrets
.env
.env.local
.env.*.local

# OS / misc
Thumbs.db
*.log

# Unrelated sibling projects kept in this directory
openclaw-in-docker/

# Session notes kept local, not committed
AI/
